# 인수인계 — 도장 시스템 전면 개편 + 등록대장 검증 (2026-07-07 세션)

> 새 대화창은 이 문서 하나로 이어받는다. 짝 문서: `WORK_GUIDE.md`(규칙) · `ROADMAP.md`(순서).

═══════════════════════════════════════════════════
## 0. 🚨 새 세션 필독 — 이번 세션에서 반복된 실수 (재발 금지)
═══════════════════════════════════════════════════

**"똑같이 만들었다"고 여러 번 말했는데 매번 또 달랐다. 원인: 한 겹만 확인하고 "됐다"고 말함.**
겹은 총 4개였는데, 매번 하나만 고치고 나머지 3개를 못 봤다:
1. **계산 함수**가 도구 값을 정확히 계산하는가
2. **진짜 앱 코드(stamps.js)를 그대로 쓰는가**, 아니면 도구가 따로 재구현했는가
3. **담기는 상자 크기**가 실제 달력 자리와 같은가 (셀 전체 vs 절반 상자)
4. **화면에 찍히는 실제 픽셀 크기**까지 같은가 (도구 미리보기 칸이 실제보다 2배 컸음)

**다음부터 "실제와 똑같습니다"라고 말하기 전에 이 4가지를 한 번에 다 확인할 것.** 하나만 보고 안심하지 말 것.

**추가로 반복된 실수:**
- **사장님이 만들어달라는 대상을 헷갈림.** "도구를 새로 만들어달라"고 했는데 엉뚱하게 "달력 틀 도구"(calendar_frame_tool.html)까지 새로 만들어버림. 사장님은 오직 **도장 미세조정 도구(stamp_adjuster.html)**만 원했음. → 지시받은 대상이 명확하지 않으면 정확히 뭘 가리키는지 먼저 확인.
- **도구와 앱이 별도로 그림을 갖고 있던 것을 뒤늦게 발견.** stamp_adjuster.html이 stamps.js와 별개로 자기만의 이미지 데이터를 내장하고 있어서, 앱 쪽 그림을 크롭해도 도구 미리보기는 안 바뀜. → 지금은 도구가 `<script src="stamps.js">`로 앱과 완전히 같은 파일을 직접 불러오도록 고쳐서 이 문제 자체가 구조적으로 재발 불가능.
- **도구 값과 실제 앱 값의 연결 통로를 처음엔 몰랐음.** 도구는 슬라이더 값을 브라우저 `localStorage`에만 저장하고, 앱은 `stamps.js`에 하드코딩된 값을 씀 — 이 둘은 자동으로 안 이어진다. 정식 통로는 도구의 **"전체 값 코드로 내보내기"** 버튼 → 복사 → Claude에게 붙여넣기. **스크린샷으로 슬라이더 숫자를 눈대중으로 읽지 말고, 반드시 이 내보내기 텍스트를 받아서 그대로(계산 없이) 반영할 것.**

═══════════════════════════════════════════════════
## 1. 현재 상태 — 도장 시스템 (완료, 안정)
═══════════════════════════════════════════════════

### 구조 (최종)
- **`stamps.js`**: 모든 도장 그림(`TEACHER_STAMPS`, `STAMPS`)과 배치값(`STAMP_LAYOUT`), 렌더 함수(`getTeacherStampSVG`, `getStudentStampSVG`, `getComboStampSVG`, `getStampIconSVG`) 전부 여기 한 곳에 있다. **공통 원칙(작업지침 5번) 그대로.**
- **`somi_ssam_home.html` / `somi_ssam_student.html`**: 달력 셀에서 `getTeacherStampSVG`/`getStudentStampSVG`/`getComboStampSVG`를 **인자 없이(size 생략)** 호출 → 부모 칸(`.ts2`, `.teacher-stamp`, `.student-stamp`)을 100% 채움. 이 세 클래스는 전부 `top:0;left:0;width:100%;height:100%`로 **셀 전체**를 기준으로 한다(예전엔 셀의 왼쪽 위 52%만 쓰는 하드코딩된 절반 상자였음 — 지금은 해제됨).
- **도장 선택 버튼**(도장 보내기 화면)은 `getStampIconSVG`로 별도 처리 — STAMP_LAYOUT 배치값 없이 중앙에 꽉 채워서 보여줌. 배지 자리를 남기려고 치우친 배치(STAMP_LAYOUT)를 버튼에 그대로 쓰면 여백이 커 보이므로 분리했다.
- **`stamp_adjuster.html`**: 도장 미세조정 도구. `<script src="stamps.js">`로 진짜 앱 파일을 그대로 불러와 쓰기 때문에, 도구 안에 그림이나 계산 로직이 따로 없다. 미리보기는 실제 학생 달력과 **완전히 같은 크기 계산식**(전체 폭 500px → 이중 패딩 20+20px → 7칸 나누기)을 그대로 재현한다.

### 겹쳐찍기(레슨완 + 상대도장)
- `getComboStampSVG(lessonType, extraType)`가 배경(레슨완, `STAMP_LAYOUT.base.teacher`)과 위(상대도장, `STAMP_LAYOUT.overlap`)를 2겹으로 그림.
- overlap 키 형식은 **`"레슨타입+상대도장"`** 조합(예: `'lessondone+star'`). 도구 내보내기 형식과 정확히 일치시켜뒀다 — 새로 값 받으면 이 키 그대로 넣으면 됨.
- 회색 결석 도장(`lessonabsent`/`lessonabsentfinal`)은 파랑/빨강 값을 그대로 공유(사장님 결정, 도구에 따로 없음). `somiStampLayout()` 함수 안에서 매핑.

### 값 갱신 워크플로 (앞으로 계속 이렇게)
1. 사장님이 `stamp_adjuster.html`에서 슬라이더로 조정
2. "전체 값 코드로 내보내기" 클릭 → 복사 → Claude에게 붙여넣기
3. Claude는 그 JSON을 **계산 없이 그대로** `stamps.js`의 `STAMP_LAYOUT`에 반영 (base.student, base.teacher, overlap 세 블록)
4. 문법 검증(`node -e "new Function(...)"`) 후 즉시 `present_files`

**현재 STAMP_LAYOUT은 2026-07-07 마지막 내보내기 값으로 최신 상태.** (student: apple91/sunflower92/cloud100/crown82/heart105/rainbow122, teacher: star50/heart45/flower44/heartfull42, overlap: 각 40/40/37/37 부근)

### 캐시 이슈 대응
`somi_ssam_home.html`/`somi_ssam_student.html`의 `<script src="stamps.js?v=...">`에 버전 쿼리를 붙여뒀다. **stamps.js 내용을 바꿀 때마다 이 버전 문자열을 갱신해서 브라우저 캐시를 피할 것.**

═══════════════════════════════════════════════════
## 2. 폐기된 것
═══════════════════════════════════════════════════
- **`calendar_frame_tool.html`**: 이번 세션에 만들었다가 폐기. 사장님이 원한 건 도장 도구뿐이었고, 달력 틀(칸 간격·숫자 크기 등)은 이미 정해진 디자인 규격이라 도구가 불필요하다는 걸 뒤늦게 확인. **outputs에서 삭제 완료, 다시 만들 필요 없음.**

═══════════════════════════════════════════════════
## 3. 등록대장(somi_ledger.html) — 이번 세션 완료 사항
═══════════════════════════════════════════════════
- **세무 내보내기 검증**: 계좌B만 정확히 필터링, 계좌 흔적 0(원칙 0), 기간 필터·정렬·회차 라벨·CSV 안전성(쉼표/따옴표 포함 이름) 전부 시뮬 검증 통과. 사장님이 실제 파일(세무사_매출)로도 눈으로 재확인 완료.
- **날짜 통일 버그 수정**: `20260701`처럼 하이픈 없이 저장된 날짜와 `2026-07-02`처럼 정상 저장된 날짜가 섞이던 문제. `getDateVal()`이 저장 시 항상 `normDate()`를 거치도록 수정 + 내보내기 4곳(세무/내확인 × 매출/매입)에서도 날짜를 `normDate()`로 한 번 더 통일. **기존에 잘못 저장된 데이터도 내보낼 때 자동 교정됨.**
- **월별 조회**: 목록 화면은 전체 기간이 다 보이고(스크롤), 이달 합계만 요약 표시. 내보내기는 기간 칸에 원하는 달을 넣어 뽑는 방식. 월별 빠른 버튼("이번달/지난달")은 아직 미도입 — 필요시 요청.

═══════════════════════════════════════════════════
## 4. 다음에 이어서 할 일
═══════════════════════════════════════════════════
- 로드맵상 도장 미세조정(F그룹 B)은 이번 세션으로 **완료** 처리 가능.
- 다음 대기 항목: **기기 중복저장 버그(F그룹 A)** — 아직 손 안 댐.
- 사장님이 도구에서 추가로 값을 조정해 내보내기하면, 위 "값 갱신 워크플로"만 따르면 됨(계산 없이 그대로 반영).
