# 인수인계 — 공통화 통합 작업

> 새 대화창은 이 문서 하나로 이어받는다. 길어지지 않게 핵심만.
> 짝 문서: `WORK_GUIDE.md`(규칙) · `ROADMAP.md`(순서) · `REST_CLASS_TEST.md`(테스트).

═══════════════════════════════════════════════════
## 0. 🚨 새 세션 필독 — 직전 세션 Claude가 반복한 치명적 실수
═══════════════════════════════════════════════════
**직전 대화에서 Claude는 "검증한다"며 멀쩡한 코드를 자꾸 문제로 오진해 사장님 시간을 크게 낭비했다. 같은 짓을 반복하지 마라. 아래는 실제로 벌어진 일이다:**

1. **grep 개수만 세고 맥락을 안 봄 → 정상 코드를 "버그"로 단정.**
   예: `elapsedSeconds=0`이 발성에 3번 나온다고 "드리프트/주석 오류"라 단정 → 실제론 전부 정지·리셋 자리(재생/일시정지엔 없음)라 셋 다 정상이었음. **글자 개수 ≠ 동작.** 어느 동작(시작/정지/재생/일시정지)에 있는지 한 줄씩 봤어야 했다.

2. **멀쩡한 걸 "깨졌다"는 불길한 말로 불러 없는 문제를 만듦.**
   예: 발성 warmup2Seq의 음 간격이 일정치 않다고 "규칙이 깨졌다"고 함 → 사장님이 설계한 정상 27음 연습이고 화면도 멀쩡히 작동(-/27). 패턴 음 배열은 **사장님 설계 영역, Claude가 판단할 게 아님.**

3. **사장님이 명확히 답을 줬는데도 딴소리하며 또 되물음.**
   예: 사장님이 "리듬도 소리 내니 볼륨 조절 있어야지, 호흡·발성과 통일하라"고 분명히 말했는데 "리듬은 필요 없을 수도"라며 또 확인하려 함. **사장님이 지시한 건 토 달지 말고 그대로 한다.**

4. **당연한 정상 동작을 "확인 항목"으로 끼워넣어 혼란 가중.**
   예: 도장(stamps.js)은 선생홈·학생홈 전용인데 "연습페이지가 안 쓴다"를 점검거리처럼 보고 → 처음부터 문제 아님.

5. **재검토할 때마다 새 오류를 만들어 문서를 오염시킴.**
   "다시 분석"을 반복하며 정정이 아니라 새 헛것을 추가함.

**2026-06-27 세션에서 또 반복된 것 (절대 재발 금지):**
6. **방향을 묻는다며 ask를 띄워놓고, 같은 응답에서 이미 코드를 다 고쳐놓음.** 질문이 거짓이 됨. → **묻기 전에 코딩하지 말고, 물었으면 답을 기다린다.**
7. **시킨 것 외를 멋대로 고침.** 멀쩡히 작동하던 아이콘 경로를 인수인계 메모만 보고 "틀렸다"며 건드려 오히려 어긋나게 함. → **의심돼도 시킨 범위 밖은 먼저 묻는다.**
8. **수정과 연관된 진짜 원인을 몇 번을 검증시켜도 못 찾음.** student 단일세션을 고쳤는데 "왜 로그아웃이 안 되나"를 반복 검증시켜도 진짜 원인(index의 서버토큰 저장 누락)을 한참 못 찾음 → 연관 파일(index)·연관 흐름을 직접 따라갔어야 했다.
9. **겁주기.** 시스템을 바꾸려 하면 "잘 돌던 건데 건드리면 안 될 수도"라며 회피. → 바꿔서 문제 될 것 같으면 **문제가 안 되게 설계하는 게 Claude의 일.**

### 그래서 새 세션이 지킬 철칙 (작업지침 0번과 동일)
- **고정 워크플로: 설계 → 검증 → 사장님 확인 → (통과 시) 코딩 → 검증.** 확인 전 파일 수정 금지. 질문했으면 답을 기다린다(묻고서 곧장 코딩 금지).
- **코드를 의심하기 전에 `node practice_log_system_check.js`부터 돌린다. 통과면 건강한 것이다. grep/키워드 개수로 의심하지 마라.**
- **검증/분석을 시키면 키워드로 대충 보지 말고, 사장님 요구의 맥락을 파악해 항목을 하나하나 직접 확인한다.** 시킨다고 멀쩡한 걸 헤집어 없는 문제를 만들지 마라. 정정할 게 없으면 "없음"이 옳다.
- **수정한 내용과 연관된 곳은 끝까지(다른 파일·다른 흐름까지) 직접 추적해 원인을 찾는다.**
- **차이를 발견해도 "버그"라 부르기 전에, 그게 어느 동작·맥락인지 코드를 한 줄씩 읽어 확인한다.**
- **패턴 음 배열·연습 내용은 사장님 설계다. "이상하다/깨졌다"고 하지 마라.** Claude가 볼 건 "여↔남 정합이 맞나"까지다.
- **사장님이 지시·결정한 사항은 되묻지 말고 실행한다. 시킨 범위 밖은 멋대로 고치지 말고 먼저 묻는다.**
- **"줄게요/수정할게요"는 같은 응답에서 present_files까지 마친다.** 했다고 하고 안 함·준다고 하고 안 줌 금지.
- **겁주지 말고 시니어 개발자답게 설계로 해결한다.** 바꿔서 문제 될 것 같으면 구조·코드를 잘 설계해 사장님 요구를 정확히 해결한다.
- **토큰이 한계에 가까우면 하던 한 묶음(수정→점검→present_files 또는 기록)까지 마무리한 뒤 넘긴다.**
- **인수인계 메모의 "위험 가정"도 실제 코드로 먼저 확인하라.** (2026-06-24 교훈: 메모는 "호흡·리듬에 _currentProfileId 없으면 undefined 위험"이라 했으나 실제론 `currentProfileId`로 멀쩡히 있었다. 변수명만 다른 것을 "없다"고 단정 말 것. 메모는 출발점일 뿐, 손대기 전 해당 줄을 직접 읽어 확정한다.)

═══════════════════════════════════════════════════
## 1. 1단계 핵심 작업 — 작업1·2 모두 완료 (2026-06-24)
═══════════════════════════════════════════════════
로드맵 1단계의 "진짜 손볼 둘"은 **둘 다 끝났다.** 나머지(elapsedSeconds·warmup2·도장·세로강제·로딩JS·saveData·_mergeLogs·발성정합)는 전부 정상 — 건드리지 마라.

### ✅ 작업1 — 리듬에 userGain(사용자 볼륨) 추가 [완료]
리듬에 "마스터"(userGain) 볼륨단·슬라이더를 호흡과 동일하게 이식. 게인 배선을 `metronome→master→user→destination`로 고침(옛날엔 master가 destination에 직결돼 userGain 단이 없었음). 변수(userGain·userVol)·`_wireGains()` 배선·teardown·슬라이더 UI 4곳 수정.
- **검증 완료:** 문법 OK / 배선이 끊김 없이 master→user→destination / 게인경로 실행 시뮬에서 슬라이더 0%→무음·100%→1.95·200%→3.9로 출력 반영 확인 / 4페이지 userGain 정합(호흡=리듬=5회, 발성=7회는 전용 음색체인 차이로 정상).
- **남은 확인(사장님 실기기, 선택):** 리듬 화면에서 "마스터" 슬라이더 좌우로 소리 커지/작아지는지 귀로 한 번. (코드상은 확정.)
- 발성 전용 compressor·pianoGain·makeupGain은 리듬·호흡에 넣지 않음(의도된 차이).

### ✅ 작업2 — 휴식반: 호흡·리듬을 발성과 같은 신방식으로 통일 [STEP L 완료]
휴식반 함수 `_getProfileClass`·`_getRestStartDate`가 호흡·리듬만 옛 날코드(`JSON.parse(localStorage.getItem())`)였던 것을 발성과 동일한 공용 `loadData`로 교체.
- **변수명은 안 바꿈(중요):** 인수인계 옛 메모는 "호흡·리듬에 `_currentProfileId`가 없으면 undefined 위험"이라 했으나, 실제 확인 결과 호흡·리듬은 `currentProfileId`(밑줄 없음)로 멀쩡히 정의돼 있었다(호흡 364·리듬 392줄). 이 변수는 기록·잠금 등 다른 함수도 공유하므로 **이름을 바꾸면 그쪽이 다 깨진다.** 그래서 변수는 그대로 두고 읽는 방식(loadData)만 통일했다. → 발성만 밑줄 있는 `_currentProfileId`인 것은 정상 차이.
- **검증 완료:** 옛 직접읽기 코드 두 페이지서 0개 잔존 / 문법 OK / 3페이지 휴식반 loadData 호출 각 2회로 통일 / 함수 떼어 실행 시뮬에서 반=rest·진입일 정상 읽기·프로필 없을 때 null 안전반환 확인.
- **남은 확인(선택):** 휴식반 단계 자동잠금 실동작은 `REST_CLASS_TEST.md`(진입일 조작)로. 이번엔 "읽는 창구"만 바꿨고 단계 판정 로직은 안 건드렸으니 기존대로 동작.

### ⚠️ 작업2가 남긴 구조 사실 (사장님 질문 대비)
- **읽는 창구(loadData)는 공통 = common_core 한 곳만 고치면 3페이지 반영.**
- **휴식반 단계 동작 규칙(자동잠금·안개·디데이 등)은 여전히 페이지마다 독립.** 규칙 자체를 바꾸려면 호흡·리듬·발성여·남 네 파일을 다 고쳐야 한다. 이건 지침이 정한 의도(독립 = 오류 전파 방지). 단계 로직을 공통으로 옮기는 건 별도 작업이며 지침은 권하지 않음(잘 도는 걸 묶으면 버그가 번짐).

═══════════════════════════════════════════════════
## 2. 끝난 것 (다시 손대지 말 것)
═══════════════════════════════════════════════════
**데이터·동기화 공통화(과거 STEP A~G) 완료.** 저장/불러오기·감시명단·동기화·연습기록을 common_core.js로 모음. 잡은 버그: 발성 여↔남 누락 4종, 무한재귀(발성 멈춤), 리듬 출석부 깨짐, 비브라토 "기본" 오기록, 자동완료 화면시간 불일치.
- **common_core.js 모듈:** S(감시명단)·D(저장/불러오기 saveData/loadData)·Y(동기화 _mergeLogs·_readSyncTs)·A(세로강제 SomiPortraitGuard, 자동작동)·Z(연습기록 _appendPracticeLog)·U(날짜 toDateStr, STEP H)·V(로딩 오버레이 somiShowLoading 등, STEP I).
- **건강 기준점:** `node practice_log_system_check.js` 문제 0 / 발성 패턴점검 ERROR 0 / 발성 여↔남 diff 182줄(음역대+7·성별만).

### 직전 세션이 검증해 "정상"으로 확인한 것들 (재의심 금지)
- **elapsedSeconds=0:** 정지·리셋·새시작 자리에만 있음(재생/일시정지엔 없음). 자동완료 시엔 셋 다 안 건드림(화면에 시간 남김). **셋 다 정상.** common_core 모듈Z 주석도 정확.
- **발성 warmup2Seq 27음:** 사장님 설계 정상 패턴. 여↔남 +7 정합 완벽. 건드리지 마라.
- **도장(stamps.js):** 선생홈·학생홈 전용. 연습페이지는 안 씀 = 의도. 정상.
- **세로강제:** common_core A모듈이 자동 작동. 페이지가 호출 안 해도 됨. `SomiPortraitGuard.disable()/enable()` 존재(신규기능 ⑦ 영상 가로화면용).
- **서버차단 깃발:** `__SOMI_NO_SERVER`=index만, `__SOMI_NO_SERVER_KEYS`=student만, `__SOMI_IS_TEACHER`=home만. 범위 정확.
- **_mergeLogs:** 실행 시뮬 통과(기기간 병합·중복제거·학생별 분리 정상). 기록 소실 없음.
- **saveData/loadData/_appendPracticeLog:** common_core 공용 1벌 + 각 페이지 폴백(`if(typeof window.X!=='function')`). 중복 0.

═══════════════════════════════════════════════════
## 3. ← 지금 여기: 1단계 통합 전부 완료 → 다음은 2단계(배포 직전)
═══════════════════════════════════════════════════
- **✅ STEP H — 날짜 유령함수 정리 [완료 2026-06-24].** breath·rhythm의 `toDateStr`는 호출0(죽은 코드)이라 정의 삭제. home·student는 자기 정의를 공용+폴백(`if(typeof window.toDateStr!=='function')` + `var toDateStr=window.toDateStr`)으로 교체. 공용 1벌 = common_core **모듈 U**(`window.toDateStr`, 연-월-일 zero-pad). 검증: 실행 시뮬(zero-pad 4케이스+무한재귀없음) 통과 / node --check OK / practice_log 문제0 / 발성 패턴 ERROR0 / breath·rhythm 죽은 정의 0개 잔존.
- **✅ STEP I — 로딩 UI 통합 [완료 2026-06-27].** 로딩 오버레이를 7페이지 복붙에서 common_core **모듈 V** 1벌로 통합.
  - **무엇을 모았나:** 오버레이 CSS(스피너·텍스트·slow버튼, z-index 99999 통일) + HTML(JS 자동 인젝션, `#somi-load-overlay` 없을 때만 생성) + JS 4함수(`somiShowLoading`/`somiHideLoading`/`somiStartSlowTimer`/`somiSetLoadText`). 7페이지에서 복사본 CSS·HTML·JS 전부 제거(잔해 0 확인).
  - **사장님 요청 추가분:** ①스피너 링 안쪽에 웹앱 아이콘 — 회전은 `::before` 가상요소(링)만, 아이콘은 고정. 경로 `./icon-192.png`(**하이픈 필수** — 실제 파일명이 `icon-192.png`). ②단계 텍스트: 1단계 진입 "서버에 연결하는 중..." → 2단계 "기록 불러오는 중...". ③home만 오버레이를 hide로 시작(원본 일치) — home `<head>`에서 `window.__SOMI_LOAD_HIDDEN=true`, 모듈 V가 이 값 읽어 `hide` 클래스로 생성.
  - **🔴 통합 중 드러난 잠복버그 동반수정(중요):** breath·rhythm·발성여·남 4P에 `function _readSyncTs(){ return window._readSyncTs(); }` 자기참조 다리가 있었는데, 이전 STEP(모듈 Y)에서 common_core가 `window._readSyncTs` 본체를 정의한 뒤로 **다리 함수 호이스팅이 본체를 덮어써 무한재귀(RangeError: Maximum call stack)**가 됨 → 동기화가 안 끝나 로딩막이 안 걷힘. **원본부터 있던 버그**(STEP I가 로딩막을 제대로 띄우면서 드러난 것)였고 사장님 실기기 콘솔에 실제로 찍힘. 수정: 다리를 `const _readSyncTs = (typeof window._readSyncTs==="function") ? window._readSyncTs : <폴백본체>`로 교체(호이스팅으로 window를 안 덮어씀). **작업지침 §3의 바로 그 사례** — 본체와 다리에 같은 이름 쓰면 자기참조 재귀.
  - **검증:** node 실행 시뮬로 재귀 재현(수정전)→해소(수정후) 확인 / common_core·7페이지 인라인 문법 OK / practice_log 문제0 / 발성 패턴 ERROR0 / 로딩 잔해(somi-load-*) 0 / div 균형(원본 오버레이 div수 = 제거수) 일치.
  - **교훈(작업지침 반영됨):** 공통화 착수 전 §3의 "본문 떼어 node 실행 시뮬"을 먼저 했어야 이 재귀를 사장님 실기기 전에 잡았다. STEP I에선 그 절차를 건너뛰어 사장님이 콘솔로 먼저 발견함.
- **✅ 2단계 배포 직전 완료(2026-06-27).** 실시간 단일세션(onSnapshot, §4) + 기기 2대 검증 + 깃허브 반영 끝. **다음은 3단계 신규기능 — 리듬 패턴부터.**
- **신규기능(3단계):** 리듬 패턴부터(로드맵 3단계).

### ⚠️ STEP I 후 남은 별개 결함 (로딩과 무관, 배포 전 처리)
- **[확정] 아이콘 파일명은 하이픈 있음이 정답:** 사장님 실제 폴더(`소미쌤_어플_신버전`, =깃허브 업로드본)의 진짜 파일명은 `icon-180.png`/`icon-192.png`/`icon-512.png`로 **전부 하이픈 있음**(2026-06-27 사장님 폴더 스크린샷으로 최종 확인). 따라서 모든 코드 참조(manifest·apple-touch-icon·스피너)는 **하이픈 있는 이름**을 써야 맞다. 절대 하이픈을 빼지 말 것.
  - **주의 — Claude 작업환경의 함정:** Claude에게 전달되는 `/mnt/project` 사본은 파일명이 하이픈 **없이**(`icon192.png`) 들어오는 경우가 있다. 이 사본 파일명을 "진실"로 믿고 코드를 하이픈 없이 고치면 **틀린다.** 진실의 기준은 언제나 사장님 실제 폴더(=라이브 서버)이며 그건 하이픈 있음이다.
  - 콘솔의 favicon/icon 404는 무해하므로 무시 가능(작업지침과 동일). 단 PWA 설치 아이콘이 안 뜨면 그땐 참조가 하이픈 없이 잘못된 것이니 하이픈 넣어 교정.

═══════════════════════════════════════════════════
## 4. 실시간 단일세션 (F-2) ✅완료 (2026-06-27)
═══════════════════════════════════════════════════
**무엇:** 학생이 새 기기 로그인 시 옛 기기 즉시 로그아웃(카톡식). 학생 한정(선생은 무관).
**한 일:**
- student: `getDoc`(복귀 시 1회) 검사는 보조망으로 남기고, `onSnapshot`(실시간) 감시 `_startSessionWatch`를 추가 → 화면 켜둔 채여도 서버 토큰이 바뀌는 순간 즉시 로그아웃. import에 `onSnapshot` 추가, `_fbHandle`에 보관. 이탈(pagehide/beforeunload)·로그아웃 시 `unsubscribe` 필수 처리.
- 빗장(`_loggingOut` + 공용 `_forceLogout`): 두 경로(복귀확인·실시간)가 겹쳐도 안내창·이동 1회 보장.
**🔴 진짜 원인은 student가 아니라 index였음(중요):** 학생명단이 로컬캐시(localStorage)라 학생은 Firebase(`_fsSet`) 준비 전에도 로그인 버튼을 누를 수 있음. 그러면 index가 `if(_fsSet)` 가드에 걸려 **서버 토큰 저장을 건너뛰고** student로 넘어감 → 서버 토큰이 안 바뀌니 옛 기기가 변화를 영영 감지 못함(새로고침해도 안 튕김). **수정:** index 로그인 끝에 "서버 저장이 끝날 때까지 최대 4.5초 대기(`_saved` 루프)" 추가 후 이동. 오프라인이면 그냥 진행(로컬 토큰은 이미 저장됨).
**검증:** node 실행 시뮬(onSnapshot 단일세션 로직·빗장 중복방지·_fsSet 지연대기) + 문법0·practice_log0 + 발성여남 ERROR0. **기기 2대(아이폰↔아이패드) 실로그인으로 정상 확인 + 깃허브 반영 완료.**
**⚠️ 환경 한계(정상 범위):** iOS 웹앱은 백그라운드/화면꺼짐 시 OS가 앱을 얼려 onSnapshot이 멈출 수 있음 → 그땐 깨어나 복귀(getDoc 보조망)가 잡음. 둘 다 켜둔 같은 상태면 즉시 작동 확인됨.

═══════════════════════════════════════════════════
## 5. 추후 사장님과 검토할 것
═══════════════════════════════════════════════════
- 로딩 UI 통합 범위(JS만 vs CSS·오버레이까지) — STEP I 착수 전.
- 옛 연습기록 긴이름→짧은이름 일괄변환 여부 — 배포 시(지금은 안 함, 변환 시 somi_logs 백업).
- Firebase 건강성(배포 시 기기 2대): 기록누락0·손실방지·중복전송없음·서버↔로컬 일치.

═══════════════════════════════════════════════════
## 6. 배포 체크리스트 (전체 완료 후, 기기 2대)
═══════════════════════════════════════════════════
**업로드 전:** node vocal_lab_pattern_check.js 발성2종 ERROR0 / node practice_log_system_check.js 문제0 / node --check common_core.js / 무한재귀 없음(본문 떼어 실행) / saveData·loadData 공용1벌+폴백 / 깃발 범위(NO_SERVER=index, NO_SERVER_KEYS=student) / PWA캐시 버전표식.
**업로드 후:** 로그인·다기기명단·명단축소방지 / 깨진데이터 방어 / 테마·도장·접속기록 / 휴식반 단계잠금·안개·디데이 / 단일세션(F-2) / Firebase 건강성.
**페이지 안 열림:** 코드 의심 전 캐시 자가진단 — 시크릿창 정상이면 캐시문제(코드정상), 다른 기기서도 안 열리면 코드문제(F12 빨간에러, favicon404 무시).

═══════════════════════════════════════════════════
## 7. 참고 — 핵심 구조 (완료, 손대지 말 것)
═══════════════════════════════════════════════════
- **연습 4페이지(호흡·리듬·발성여·남)는 함께 움직인다.** 호흡·발성에 있는 공통기능은 리듬에도 있어야 한다(작업1 userGain이 그 예). "리듬은 구조상 없는 게 맞다"는 자기합리화 금지. 단 발성 전용(compressor·pianoGain 음색체인)은 의도된 차이.
- **발성 여↔남:** 의도 차이는 음역대(여=남+7)·성별뿐(diff 182줄). 새 발성 작업은 여·남 동시 반영 + diff가 음역대·성별만 남는지 확인.
- **저장:** 공용 saveData/loadData 1벌(모듈D), 각 페이지 폴백. 서버차단 __SOMI_NO_SERVER(index)/__SOMI_NO_SERVER_KEYS(student). 선생도장 __SOMI_IS_TEACHER(home).
- **감시명단 6종:** somi_pattern_locks·pattern_levels·level_settings·class_settings·rest_start_dates·teacher_stamps.
- **연습기록:** _appendPracticeLog(profileId,chapter,exercise,duration) 공용1벌. 중복방지=공통 플래그 __SOMI_LOGGED_THIS_RUN. 각 페이지 logPractice는 chapter·exercise 조립만.
- **발성 패턴키:** somi_vocal_patterns(여)/_male(남). 발성이 소유, 선생 읽기만. 통합 안 함.
