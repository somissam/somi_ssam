# 인수인계 (HANDOVER) — 2026-07-08

> **다음 대화창은 이 문서부터 읽는다.** 이 문서는 항상 "최신 인수인계 하나"로 유지한다.
> 규칙=`WORK_GUIDE.md`, 순서=`ROADMAP.md`, 완료이력=`COMPLETED.md`(과거 통합 상세도 여기 흡수됨),
> 등록대장 설계=`등록대장_설계도.md`, 휴식반 테스트=`REST_CLASS_TEST.md`.

---

## ⚡ 지금 상태 (한 줄)
8월 1일 개업 대비 핵심(등록대장·회차·안전장치) 완료. 공지 기능·발성 UI 정리·E그룹 정리(②③)·앱 전체 탭전환 '맨 위로'까지 완료.
**리듬 패턴 완성은 완전 나중으로 미룸(사장님 확정). 다음 = 로드맵 '▶ 다음 작업 후보' 참조(로딩 꿀팁 등).**

---

## 이번 세션(2026-07-08 오후)에 한 일

### 1. 전체 공지 기능 (로드맵 D) — 완료
선생님이 학생관리 탭에서 공지 작성(여러 개 가능) → 학생홈 상단에 핑크 배너로 표시. Firebase `somi_notice`.
- 선생님: 입력칸(1줄+자동확장)+종료일(기본 오늘+1)+[공지 추가], 목록에서 개별 [수정]·[내리기/올리기]·[삭제], 게시중은 핑크 배경.
- 학생: 홈 최상단 핑크 배너(`📢 공지 [내용]`), 여러 개면 세로로 쌓임.
- common_core 모듈 N(`somiGetActiveNotices`·`somiNoticeBannerHTML`·`SOMI_NOTICE_PINK` 등).
- 상세는 COMPLETED.md 참조.

### 2. 발성 UI 정리 — 완료 (vocal_lab.html · _male.html)
- 다크모드 비활성 발음 밝게(#5a5048→#9a8f82)
- 음버튼 그리드 폭을 라벨박스(364px)에 통일 — 노트북 넘침/폰 작음 해결
- 기본모드 상/하행 버튼 grid 균등분할로 폭 통일(하행이 상행보다 넓던 것 수정)

### 3. E-② Shift클릭 공통화 — 완료
- `somiRangeIndices` 공용 계산기, 패턴표·레벨표 둘 다 패턴ID 방식으로 통일, 레벨표 포커스 테두리 제거.

### 4. E-③ 드롭박스 공통화 — 완료
- `somiAttachDropdown`으로 계좌·반···· 3곳 통일. 레벨 드롭다운은 독립 유지(위험해서 안 합침).

### 5. 스크롤 문제 수정 — 완료
- `somiScrollTop`(전환시 맨위)·`somiPreserveScroll`(재렌더 자리유지) 공통함수 신설.
- 학생 카드 누르면 상세 맨 위부터 보이게, 계좌·반 등 선택해도 맨위로 안 튀고 자리 유지.

### 6. 로딩 속도 개선 — 보류
- 진단만 함(중복 firebase 연결·순차 대기·신규학생 캐시 미적용). 사장님이 "지금 상태 유지"로 보류.
- 개선하려면 방향: **"로컬 데이터로 화면 먼저 → 서버 동기화는 뒤에서 조용히"**. 지금은 로컬이 있어도
  로딩 커튼 치고 서버 다 받을 때까지 학생을 세워둠. `_mergeLogsToServer` 덕에 기록 유실 걱정은 이미 해소됨.

### 7. 앱 전체 탭 전환 '맨 위로' 적용 — 완료
- 선생님 탭(학생관리↔패턴↔설정), 학생 탭(홈↔설정) 전환 시 `somiScrollTop()` 호출 추가.
- 호흡·리듬·발성 연습 진입 시(`startExercise`) 맨 위로.
- 연습 종료 후 목록 복귀(`closePractice`)는 **셋 다 "스크롤 자리 기억→복원" 방식으로 통일**
  (원래 발성에만 있던 `sessionStorage` 방식을 호흡·리듬에도 이식. 사장님 지시로 통일 확정).
  - 호흡 키: `somi_breath_scroll`, 리듬 키: `somi_rhythm_scroll`, 발성 키: `somi_vocal_scroll`(기존).

### 8. 리듬 패턴 완성 — 로드맵에서 제외 (완전 나중으로 미룸)
- 사장님 확정: "리듬 만들기는 완전 나중에". 3단계 신규기능 본목록·다음 작업 후보에서 제거.
  다시 진행할 때는 ROADMAP.md `⏸ 보류` 섹션 참고(설계 방향: 호흡·발성처럼 패턴 채움, 독립 코딩, userGain 작업과 병행).

---

## 이번 세션 수정 파일 (교체 시 같은 폴더에 함께 업로드)
- **common_core.js** (모듈 N 공지, somiAttachDropdown, somiRangeIndices, somiScrollTop, somiPreserveScroll) — 항상 동봉
- **somi_ssam_home.html** (공지 작성/목록, 드롭다운·Shift 공통화 적용, 스크롤 적용, 탭전환 맨위로)
- **somi_ssam_student.html** (공지 배너 + 수신 KEYS, 탭전환 맨위로)
- **somi_ssam_breath.html** (발성 UI 정리 무관, 연습 진입 맨위로 + 스크롤 기억 방식 이식)
- **somi_ssam_rhythm_groove.html** (연습 진입 맨위로 + 스크롤 기억 방식 이식)
- **somi_ssam_vocal_lab.html** / **somi_ssam_vocal_lab_male.html** (발성 UI 정리 + 연습 진입 맨위로 추가)

※ 그 외 파일(index·ledger·stamps)은 이번 세션 변경 없음.

---

## ★ 다음 세션이 이어받을 일

### 로드맵 '▶ 다음 작업 후보' 순서대로 (ROADMAP.md 참조)
1. ⑥ 로딩 꿀팁
2. ① 도장 인센티브
3. ② 목관리 팁 탭
4. ③ 노래연습 기록 탭
5. ⑦ 영상 가로 전체화면
6. ④ 폰 캘린더 안내(난이도↑, 방향 결정 필요)
7. ⑤ 자동 안내발송(신인프라, ④와 연계)

### 방침 변경 (사장님 확정)
- **E-① 선생님 모드 파일 쪼개기 = 취소.** 등록대장을 새로 만들 때 따로 빼서 큰 문제 해결됨.
  앞으로 새 파일은 처음부터 쪼개서 설계하고, 그러고도 무거우면 그때 쪼갬.
- **리듬 패턴 완성 = 완전 나중으로 미룸.** 본목록·다음 작업 후보에서 제외됨. 사장님이 다시 꺼낼 때까지 건드리지 않음.

---

## ⚠️ 미해결 노티 (잊지 말 것)
- **발성 버튼 그리드 학생기기 실측 미완** — 사장님 노트북은 만족. 폰 등에서 라벨박스보다
  작거나 글자 삐져나옴 가능성 남음(작업환경 네트워크 off라 실측 못함). 학생 피드백 나오면 그 기기 기준 재조정.
- **아이콘 이미지 누락**(이전 세션부터 이월) — 일부 학생 웹앱 홈화면 아이콘 누락(안드·아이폰 사례).
  manifest.json·icon*.png 참조 경로 점검 필요. 미진단.

---

## 문서 구조 (이 세션에서 재편·확정 — 딱 5개)
- **`WORK_GUIDE.md`** — 규칙 (어떻게 일하는가)
- **`ROADMAP.md`** — 순서 (무엇을 언제 만드는가)
- **`HANDOVER.md`** (이 문서) — 최신 인수인계 하나. 다음 세션이 가장 먼저 읽음. 매번 최신으로 교체.
- **`COMPLETED.md`** — 완료 작업 누적 이력 + 과거 통합 상세 + 핵심 구조 + 배포 체크리스트. "이거 이미 했나?" 창고.
- **`등록대장_설계도.md`** — 등록대장(돈 관련) 설계 원본. 보관.
- **`REST_CLASS_TEST.md`** — 휴식반 테스트 절차.

> 이전 인수인계는 COMPLETED.md에 흡수됨. 다음 세션부터 인수인계는 이 HANDOVER.md 하나만 최신으로 갱신하면 됨(새 날짜 파일 만들지 말 것).
