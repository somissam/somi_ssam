# 소미쌤 앱 — 로드맵 (할 일 순서표)

> 순서표. 규칙은 `WORK_GUIDE.md`, 최신 인수인계는 `HANDOVER.md`, 완료 이력은 `COMPLETED.md`.
> 등록대장 설계는 `등록대장_설계도.md`, 휴식반 테스트는 `REST_CLASS_TEST.md`.
> 작업할 때만 꺼내 읽는다(평소 토큰 절약).

---

## ⚡ 지금 상태 (한 줄)
1·2단계 완료. 3단계 진행 중. **7월 최우선(등록대장·회차)·안전장치 완료.** F그룹 후속 A·B·C 완료.
공지 기능(D)·발성 UI 정리·E그룹 정리(②③)·앱 전체 탭전환 '맨 위로' 완료. **다음 = ▶ 다음 작업 후보 참조.**
상세: `HANDOVER.md`, 완료분: `COMPLETED.md`.

---

## 🔴 7월 최우선 (8월 1일 D-day) — ✅ 전부 완료
> 상세 설계: `등록대장_설계도.md`. 완료 이력: `COMPLETED.md`.
등록대장(매출·매입) 독립 파일, 계좌B 세무자료, 레슨완 회차 카운팅(4/8회·마지막 빨강·당일결석 회색),
세무 내보내기, 등록대장 안전장치(서버백업·병합·삭제기록), 월별보기 — 전부 완료.
세무(현실): 전화상담 → 계약서 후 업종추가+사업자통장 → 8월 1일 개업+배포.

## 큰 순서 (사장님 확정: 1 → 2 → 3)
1. **남은 통합 정리** ✅완료
2. **배포 직전**(실시간 단일세션 + 기기2대 검증 + 깃허브) ✅완료(2026-06-27)
3. **← 지금: 신규기능**

---

## 3단계 신규기능 — 진행 현황

### ✅ 완료된 것 (상세는 COMPLETED.md)
- **F그룹 후속 A**(기기 중복저장), **B**(도장 미세조정·클릭영역), **C**(연습기록 동기화) — 완료
- **D. 전체 공지 기능** — 완료(2026-07-08). 선생 작성(여러 개)→학생홈 상단 핑크 배너→Firebase `somi_notice`.
- **발성 UI 정리** — 다크모드 비활성발음 밝기, 음버튼 그리드 라벨박스 폭 통일, 상/하행 버튼 grid 균등분할
- **E-② 체크박스 Shift클릭 공통화** — `somiRangeIndices` + 두 표 ID방식 통일 + 테두리 제거
- **E-③ 드롭박스 공통화** — `somiAttachDropdown`으로 계좌·반···· 3곳 통일(레벨은 독립 유지)
- **스크롤 공통함수** — `somiScrollTop`(전환시 맨위)·`somiPreserveScroll`(재렌더 자리유지). 선생홈 학생상세·옵션선택에 적용.
- **앱 전체 탭 전환 '맨 위로'** — 완료(2026-07-08). 선생님 탭(학생관리↔패턴↔설정), 학생 탭(홈↔설정) 전환 시 맨 위로.
  호흡·리듬·발성 연습 진입 시 맨 위로. 연습 종료 후 목록 복귀는 셋 다 "고르던 카드 자리로 스크롤 복원"(발성 기존 방식으로 통일).

### ⏸ 보류
- **로딩 속도 개선** — 진단 완료(중복 firebase 연결·순차 대기·신규학생 캐시 미적용).
  사장님이 "지금 상태 유지"로 보류. 개선하려면 "로컬 먼저 표시→동기화는 뒤에서" 방향(설명은 HANDOVER 참조).
- **E-① 선생님 모드 파일 쪼개기** — **취소.** 등록대장을 새로 만들 때 따로 빼서 큰 문제 해결됨.
  → 방침: 추후 새 파일은 처음부터 쪼개서 설계, 그러고도 무거우면 그때 쪼갬.
- **E-④ 발성 라벨박스 넘침** — 발성 UI 정리에서 함께 처리됨(음버튼 그리드 폭 통일).
- **리듬 패턴 완성** — **완전 나중으로 미룸(사장님 확정, 2026-07-08).** 본목록에서 제외.
  다시 꺼낼 때 참고: 호흡·발성처럼 리듬 패턴 채움(독립 코딩), userGain 작업과 함께 보면 좋음.
- **발성 `warmup` 서랍 이름 정리** — 발성 패턴의 `warmup1/2/3Seq` 칸은 원래 목풀기 전용으로 만든 것인데,
  7th·iaiaiai 등 목풀기 아닌 연습도 같은 칸을 재활용해 넣으면서 이름(warmup=목풀기)과 내용이 안 맞음.
  중립적 이름(slot/preset 등)으로 바꾸자는 논의. **미룸 이유**: 목풀기 모듈 추가 작업이 아직 진행 중이라
  지금 이름 바꾸면 새 이름·옛 이름 섞여 더 헷갈림. **추가 작업 다 끝난 뒤** 진행 권장.
  얽힌 곳(한꺼번에 바꿔야): 변수 `wu_`, 섹션명 `E-WARMUP`, 필드 `warmupN`/`warmupNSeq`, 모드 매핑 `wu_getSeq`.
  두 파일(vocal_lab·_male) 동시 적용 + 화면엔 안 보이는 내부 이름이라 학생·선생 화면 영향 없음.

### ▶ 다음 작업 후보
1. **⑥ 로딩 꿀팁** — 로딩함수 이미 있음 → common_core에 꿀팁 배열+랜덤 얹기.
2. **① 도장 인센티브** — stamps.js·캘린더 도장 있음 → "모으면 보상" 규칙 추가.
3. **② 목관리 팁 탭** — 새 페이지(반독립), common_core 로드+내용 독립.
4. **③ 노래연습 기록 탭** — 새 페이지 + 기록은 공용 데이터.
5. **⑦ 영상 가로 전체화면** — 특정 영상만 가로. 진입 시 `SomiPortraitGuard.disable()`, 나올 때 `.enable()`.
6. **④ 폰 캘린더 안내**(난이도↑) — 방향 갈림(.ics/구글캘린더/앱내입력) 결정 후.
7. **⑤ 자동 안내발송**(신인프라) — 서버 필요. ④와 연계.

### 미해결 노티 (다음 세션이 챙길 것)
- **A. 발성 버튼 그리드 학생기기 실측 미완** — 사장님 노트북은 만족. 폰 등 학생기기에서
  라벨박스보다 작거나 글자 삐져나옴 가능성 남음(네트워크 off라 실측 못함). 학생 피드백 나오면 그 기기 기준 재조정.
- ~~**B. 아이콘 이미지 누락**~~ → **[해결 2026-07-09]** 원인=참조 파일명 불일치(코드는 하이픈 없는 `icon180.png`를
  부르는데 라이브 폴더 실물은 하이픈 있는 `icon-180.png`). 참조 8곳 전부 하이픈으로 통일 + 아이콘 3장 정사이즈 재단.
  상세는 `COMPLETED.md` 「아이콘 3종 참조 통일 + 확대」 참조.

---

## 3단계 신규기능 본목록 (권장 순서)
> 리듬 패턴 완성은 완전 나중으로 미룸(사장님 확정) — 이 목록에서 제외. 다시 진행할 때 ⏸ 보류 섹션 참고.
1. **⑥ 로딩 꿀팁** — 로딩함수 이미 있음 → common_core에 꿀팁 배열+랜덤 얹기.
2. **① 도장 인센티브** — stamps.js·캘린더 도장 있음 → "모으면 보상" 규칙 추가.
3. **② 목관리 팁 탭** — 새 페이지(반독립), common_core 로드+내용 독립.
4. **③ 노래연습 기록 탭** — 새 페이지 + 기록은 공용 데이터.
5. **⑦ 영상 가로 전체화면** — 특정 영상만 가로. 진입 시 `SomiPortraitGuard.disable()`, 나올 때 `.enable()`.
6. **④ 폰 캘린더 안내**(난이도↑) — 방향 갈림(.ics/구글캘린더/앱내입력) 결정 후.
7. **⑤ 자동 안내발송**(신인프라) — 서버 필요. ④와 연계.

---

## 통합 대상 그룹 (실측 기준) — ✅정상(건드리지 마라) / ★작업
- 로딩 UI: ✅ common_core 모듈 V로 통합 완료(STEP I)
- 날짜 toDateStr: ✅ 유령 삭제 + 공용+폴백(STEP H, common_core 모듈 U)
- 날짜 입력: ✅ common_core 공통함수 3종(parseNumericDate/makeNumericDateInput/attachNumericDate)
- 드롭다운 토글: ✅ `somiAttachDropdown`(pm-방식 3곳). 레벨(lv-menu-open)은 독립 유지
- Shift 범위선택: ✅ `somiRangeIndices` 공용 + 두 표 ID방식 통일
- 스크롤 제어: ✅ `somiScrollTop`·`somiPreserveScroll`(선생홈·학생홈 적용, 호흡·리듬·발성 연습 전환 전체 완료)
- 오디오 엔진: ✅ 리듬 userGain 추가 완료. 발성 음색체인은 발성전용 정상
- 휴식반: ✅ 읽는 창구 loadData 통일. 단계 규칙은 페이지별 독립
- 연습 실행 골격: ❌ 통합금지(독립 유지)

## 직전 세션들이 검증해 "정상"으로 확정한 것 (재의심 금지)
elapsedSeconds(정지·리셋에만) / warmup2(정상 설계) / 도장(선생·학생홈 전용) / 세로강제(자동작동) /
_mergeLogs(병합 정상) / saveData·loadData(공용1벌+폴백) / 서버차단 깃발 범위 / 발성 여↔남 정합(182줄).
