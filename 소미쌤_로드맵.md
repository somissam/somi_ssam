# 소미쌤 앱 — 로드맵 (할 일 순서표)

> 순서표. 규칙은 `소미쌤_작업지침.md`, 통합 상세는 `인수인계_데이터통합.md`, 휴식반 테스트는 `휴식반_테스트_방법.md`.
> 작업할 때만 꺼내 읽는다(평소 토큰 절약).

---

## ⚡ 지금 상태 (한 줄)
데이터·동기화 공통화(STEP A~G) + 작업1·2 + STEP H + **STEP I(로딩 UI 통합) 완료(2026-06-27).** **1단계 통합 전부 끝남 → 다음은 2단계(배포 직전: 단일세션 F-2 + 기기 2대 검증 → 깃허브).**

## 큰 순서 (사장님 확정: 1 → 2 → 3)
1. **남은 통합 정리** ✅**완료**(작업1·2·STEP H·I 전부 끝).
2. **← 지금: 배포 직전** — 실시간 단일세션(옛 기기 즉시 로그아웃) + 기기 2대 검증 → 깃허브.
3. **신규기능** — 리듬 패턴부터.

> ★ 통합(가구 정리)을 끝내고 → 배포(이사)로 못 박은 뒤 → 신규기능(새 가전). 신규를 배포 전에 넣으면 버그 원인 추적이 꼬임.

---

## 1단계 — 남은 통합

### ✅ 작업1 — 리듬 userGain(사용자 볼륨) 추가 [완료 2026-06-24]
호흡 기준으로 리듬에 "마스터" 볼륨단·슬라이더 이식, 배선 metronome→master→user→destination. 검증 완료(상세 인수인계 §1).

### ✅ 작업2 — 휴식반 호흡·리듬을 신방식으로 통일 [STEP L 완료 2026-06-24]
호흡·리듬의 `_getProfileClass`·`_getRestStartDate`를 발성과 같은 공용 loadData로 교체. 변수명(currentProfileId)은 그대로(다른 함수와 공유). 검증 완료(상세 인수인계 §1).

### ✅ 1단계 통합 전부 완료 (안전한 것부터)
- **✅ STEP H — 날짜 유령함수 정리 [완료 2026-06-24].** breath·rhythm의 `toDateStr`는 호출0(죽은 코드) → 삭제. home·student는 공용+폴백(`if(typeof window.toDateStr!=='function')`)으로 교체. 공용 1벌은 common_core 모듈 U(`window.toDateStr`). 점검 0문제.
- **✅ STEP I — 로딩 UI 통합 [완료 2026-06-27].** 로딩 오버레이 CSS·HTML·JS(somiShowLoading/Hide/StartSlowTimer + setLoadText)를 7페이지 복붙에서 common_core **모듈 V** 1벌로 통합. 오버레이 HTML은 JS로 자동 인젝션(없을 때만). 사장님 요청 추가분: ①스피너 링 안쪽에 웹앱 아이콘(`./icon-192.png` — 실제 파일명이 하이픈 있음이라 하이픈 그대로 둘 것, 빼지 말 것) ②단계 텍스트("서버에 연결하는 중"→"기록 불러오는 중") ③home은 `window.__SOMI_LOAD_HIDDEN=true`로 hide 시작(원본 일치). **통합 중 드러난 잠복버그 동반수정:** breath·rhythm·발성여·남 4P의 `function _readSyncTs(){return window._readSyncTs();}` 자기참조 무한재귀(common_core가 `_readSyncTs` 본체를 이미 정의 → 페이지 다리함수 호이스팅이 본체를 덮어써 RangeError) → `const _readSyncTs = window._readSyncTs`(폴백 포함)로 교체. 검증: node 실행 시뮬로 재귀 재현/해소 확인, 문법·practice_log 0·발성 패턴 0, 로딩 잔해 0, div 균형.
- **그룹7 연습 실행 골격(startExercise·logPractice 등): 독립 유지 = 통합 금지.**

## 2단계 — 배포 직전 ← 지금 여기
- **실시간 단일세션(F-2):** 새 기기 로그인 시 옛 기기 즉시 로그아웃. getDoc→onSnapshot. **기기 2대로 배포 직전 검증.** 상세 인수인계 §4.
- 다기기 검증 + PWA/캐시 버전 표식.

## 3단계 — 신규기능 (권장: 리듬패턴 → ⑥로딩꿀팁 → ①도장인센티브 → ②목관리탭 → ③노래연습기록 → ⑦영상가로화면 → ④캘린더 → ⑤자동안내)
1. **리듬 패턴 완성** — 호흡·발성처럼 리듬 패턴 채움(독립 코딩). userGain 작업1과 함께 보면 좋음.
2. **⑥ 로딩 꿀팁** — 로딩함수 이미 있음 → common_core에 꿀팁 배열+랜덤 얹기. STEP I와 함께.
3. **① 도장 인센티브** — stamps.js·캘린더 도장 있음 → "모으면 보상" 규칙 추가.
4. **② 목관리 팁 탭** — 새 페이지(반독립), common_core 로드+내용 독립.
5. **③ 노래연습 기록 탭** — 새 페이지 + 기록은 공용 데이터.
6. **⑦ 영상 가로 전체화면** — 특정 영상만 가로. 진입 시 `SomiPortraitGuard.disable()`, 나올 때 `.enable()`.
7. **④ 폰 캘린더 안내** (난이도↑) — 웹앱은 폰 캘린더 직접접근 불가. 방향 갈림(.ics/구글캘린더/앱내입력) 결정 후.
8. **⑤ 자동 안내발송** (신인프라) — 브라우저만으론 불가, 서버 필요. ④와 연계.

---

## 통합 대상 그룹 (실측 기준)
> ✅정상(건드리지 마라) / ★작업(진짜 손볼 것)
- 로딩 UI: ✅ common_core 모듈 V로 통합 완료(STEP I, 2026-06-27). 오버레이 CSS·HTML·JS 1벌 + 스피너내 아이콘 + 단계텍스트 + home hide시작
- 날짜 toDateStr: breath·rhythm 유령 삭제 + home·student 공용+폴백 ✅(STEP H, 공용=common_core 모듈 U)
- 오디오 엔진: 리듬 userGain 추가 완료 ✅(작업1). 발성 음색체인(compressor 등)은 발성전용 ✅
- 동기화 화면부: _clearEndedFog만 동일, 나머지 화면별 ✅
- 휴식반: 호흡·리듬 읽는 창구 loadData로 통일 완료 ✅(작업2). 나머지 7종 동일 ✅
- 연습 실행 골격: 독립 유지 ❌통합금지

## 직전 세션이 검증해 "정상"으로 확정한 것 (재의심 금지)
elapsedSeconds(정지·리셋에만, 셋 동일) / warmup2(정상 설계 패턴) / 도장(선생·학생홈 전용) / 세로강제(자동작동) / _mergeLogs(병합 정상) / saveData·loadData(공용1벌+폴백) / 서버차단 깃발 범위 / 발성 여↔남 정합(182줄).
