# 소미쌤 앱 — 로드맵 (할 일 + 통합 목록)

> 이 파일은 "무엇을 언제 어떻게 만들지"의 순서표다. (규칙은 `소미쌤_작업지침.md` 참조)
> 프로젝트에 보관 → 작업할 때만 꺼내 읽음(평소 토큰 절약).
> 각 항목은 "요리 순서"처럼 위에서부터 따라 하면 그대로 적용되게 적었다.

---

## A. 큰 순서 (갱신됨 — 통합을 앞으로)

> 결론 변경: "통합을 맨 마지막"은 비효율(신규기능 쌓을수록 복붙 더미가 커짐).
> → **신규 기능을 쌓기 전에 안전한 통합부터** 한다.

1. **리듬 패턴 완성** → 연습 패턴 마무리 (현재 여기)
2. **데이터/동기화 공통화** ← **지금 결정한 다음 작업.** 새 대화창 + `인수인계_데이터통합.md` 사용
3. **나머지 안전 통합** (로딩·날짜·오디오·동기화) — `common_core.js`로
4. **신규 기능 추가** (아래 B) — 이제 공통 파일 한 곳만 작성하면 전 페이지 적용
5. **까다로운 통합**(휴식반)은 관련 기능 손댈 때 함께

---

## B. 신규 기능 — 추천 순서대로

> 추천 순서 = 토대가 이미 있어 쉽고, 효과가 빠른 것부터.
> 권장 진행: 6 → 1 → 2 → 3 → 7 → 4 → 5

### B-1. 리듬 패턴 완성 (먼저)
- 호흡·발성과 같은 방식으로 리듬 패턴을 채운다. 패턴은 독립 코딩.
- 끝나면 `node vocal_lab_pattern_check.js`로 점검(리듬은 대상 아니지만 발성 2종 정합성 확인).

### B-2. ⑥ 로딩 꿀팁 (롤 로딩화면식) — 가장 먼저 추천
- 이유: 이미 모든 페이지에 동일한 로딩 함수(`somiShowLoading` 등)가 있음 → 공통 토대 검증의 첫 시험대.
- 방법:
  1. `common_core.js`에 "꿀팁 배열 + 로딩 중 랜덤 표시" 모듈을 추가한다.
  2. 기존 `somiShowLoading`이 호출될 때 꿀팁 한 줄을 같이 띄우게 연결.
  3. 꿀팁 문구는 배열 하나로 관리(나중에 추가/수정 쉽게).

### B-3. ① 도장 인센티브 제도
- 이유: 이미 `stamps.js`(도장)와 캘린더 도장 표시가 있음 → 그 위에 "모으면 보상" 규칙만 얹음.
- 방법:
  1. 도장 적립/달성 규칙을 `common_core.js`(또는 stamps와 짝이 되는 공통부)에 둔다 — 어느 연습을 하든 같이 쌓여야 하므로 공통.
  2. 달성 시 학생홈에서 보상/축하를 표시.

### B-4. ② 목관리 팁 정보 탭
- 성격: 새 페이지(반독립). 공통 토대(common_core.js)는 로드하되, 내용은 독립.
- 방법: 새 탭 페이지 1개 추가 → 상단 탭 메뉴에 연결 → 글 목록/상세 구조.

### B-5. ③ 노래연습 기록 탭
- 성격: 새 페이지(반독립) + 기록은 공통 데이터(localStorage `somi_*`)에 저장.
- 방법: 연습기록과 같은 저장 방식 재사용 → "오늘의 연습 성과"에 노래연습도 합산되게.

### B-6. ⑦ 영상 가로 전체화면 (유튜브 전체화면식)
- 주의: 앞서 지운 "앱 전역 가로 보정"과 다름. 이건 **특정 영상/화면에서만** 가로 꽉채움.
- 방법:
  1. 영상/전체화면 진입 시 `window.SomiPortraitGuard.disable()` 호출 → 세로강제(A) 일시 해제.
  2. 빠져나오면 `.enable()`로 복구.
  3. 정식 가로 레이아웃이 필요하면 그때 백지에서 새로 설계.

### B-7. ④ 폰 캘린더 레슨일정 읽어 재등록 안내 — 난이도 높음
- 제약: **웹앱은 폰 기본 캘린더에 직접 접근 불가**(특히 iOS 사파리). 우회 필요.
- 먼저 결정할 것(방향 갈림): ⓐ .ics 파일 수동 가져오기 / ⓑ 구글 캘린더 연동 / ⓒ 앱 내 일정 직접 입력.
- 방향 정해진 뒤 설계 시작.

### B-8. ⑤ 재등록 자동 안내 발송(보호자 포함) — 신인프라
- 제약: 자동 발송은 브라우저만으론 불가 → 별도 서버/알림(문자·이메일·푸시) 필요.
- ④와 연계. 인프라 선정 후 진행.

---

## C. 기존 복붙 통합 (마지막 단계) — 그룹별 목록

> 지금 페이지마다 복붙돼 있는 "공통 성격" 함수들. 마지막에 한 그룹씩 `common_core.js`로 모은다.
> ✅=완전 동일(통합 쉬움) / ⚠️=페이지마다 차이(통합 전 검증) / ❌=통합 안 함(독립이 정상)
> 대상 페이지: home / student / breath(호흡) / rhythm(리듬) / vocal_lab(발성 2종)

### 그룹 1. 로딩 UI ✅
`somiShowLoading` `somiHideLoading` `somiStartSlowTimer`
→ 완전 동일. ⑥ 로딩 꿀팁과 함께 통합하면 자연스러움. **가장 먼저 통합 추천.**

### 그룹 2. 날짜 유틸 ✅
`toDateStr`
→ 들여쓰기만 다르고 로직 동일.

### 그룹 3. 오디오 엔진 (대체로 동일, 발성 일부 ⚠️)
`initAudio`(동일) `ensureAudio` `_buildCtx`(발성만 다름) `_audioDiag`(발성만 다름)
`_teardownCtx` `_unlockAudio` `_setIOSSession` `_playSilentElement` `_stopSilentElement`
`_startClockWatchdog` `_gestureRebuild` `_makeProbes` `_closeProbes` `_adoptProbe`
`_storedSR` `_rememberSR` `_srForAttempt`
→ 소리 재생·iOS 오디오 잠금 해제 기반. 통합 시 발성 차이 부분만 확인.

### 그룹 4. 동기화 (일부 ⚠️)
`_checkAndResync`(페이지마다 다름) `_afterSync` `_readSyncTs` `_recordAccess`
`_mergeLogs`(동일) `_clearEndedFog`
→ localStorage·Firebase 맞추기.

### 그룹 5. 데이터/동기화 저장 ⚠️ (다음 작업 — 인수인계 문서 있음)
`saveData` `loadData` + 동기화(`_SYNC_WATCH_KEYS`/`somi_sync_meta`/`_checkAndResync`) + `logPractice`/`_mergeLogs`
→ 결론: 공통화 **가능하고 그게 더 나음**. 단 "통째로"가 아니라 **역할별 3층 분리**:
   ① 순수 저장(완전 공통) ② 공유신호(선생만 도장 찍기로 분기) ③ 공유키 목록(공통).
→ 핵심 주의: home의 saveData만 "동기화 도장(ts)"이 있음 → 이 차이를 살려야 함(선생→학생 반영).
→ 발성 2종은 saveData 함수가 없고 localStorage 직접호출 → 가장 신중히 교체.
→ **상세 절차는 `인수인계_데이터통합.md` (STEP A~G + 누락방지 체크리스트) 참조.**

### 그룹 6. 휴식반/종료반 자동관리 ⚠️ (가장 까다로움)
`_isRestClass` `_isEndedClass` `_getRestStage`(동일) `_getRestDays` `_getRestStartDate`
`_getProfileClass` `_showRestModal` `_renderRestDday` `_maybeShowRestStagePopup`
`_applyEndedFog`(페이지마다 다름) `_clearEndedFog`
→ 규칙(공통)과 화면표시(페이지별)가 섞임. 통합하려면 둘을 분리해야 함.
   당장은 규칙대로 "새 페이지마다 빠짐없이 포함"을 유지하고, 통합은 최후에.

### 그룹 7. 연습 실행 골격 ❌ (통합하지 말 것)
`startExercise` `stopExercise` `closePractice` `togglePlay` `startEngine` `stopEngine`
`pauseEngine` `schedulerTick` `scheduleStop` `timerFrame` `buildPracticeUI`
`renderPatternCards` `currentPat` `setInfoBox` `labRefresh` `logPractice` `hardReset`
`getPatternLevel` `isLocked`
→ 이름은 같아도 연습마다 내용이 달라야 정상(호흡과 발성의 동작이 다름). **독립 유지.**

### 통합 작업 권장 순서
**데이터(그룹5) 먼저** → 1(로딩) → 2(날짜) → 3(오디오) → 4(동기화) → 6(휴식반, 최후). 7은 건드리지 않음.
→ 데이터는 `인수인계_데이터통합.md`의 STEP A~G를 따른다(역할별 3층 분리).

---

## D. 미해결 확인 사항 (잊지 말 것)
- **`logPractice` 페이지별 해시 차이**(breath/rhythm/발성 제각각)가 연습별 의도인지 복붙 어긋남인지 검증. (데이터 통합 STEP G에서)
- **index/student의 saveData**가 home·breath와 또 다름 → 차이 원인 규명 후 공통+분기 처리. (STEP D)
