# 인수인계 — 데이터/동기화 공통화 작업

> 새 대화창에서 이 작업을 진행한다. 이 문서 하나만 보고도 누락 없이 끝낼 수 있게 작성함.
> 최우선 원칙(절대 위반 금지):
> **① 작업 후에도 지금 결과물이 100% 그대로 나와야 한다(기능·출력 무변화).**
> **② 공통화할 부분을 하나도 누락하지 않는다.**
> **③ 항목을 잘게 나눠, 한 조각씩 검증하며 안전하게 통합한다.**
> 마지막 아웃풋은 사장님이 별도 백업해 둠. 작업 전 outputs와 백업 대조 권장.

---

## 0. 시작 전 체크리스트 (반드시)
1. outputs가 비었으면 → `/mnt/project`를 outputs로 복사 후 그 위에서 작업. (단 백업/최신본이 있으면 그것 우선)
2. 작업 대상 파일을 건드리기 전, 각 파일의 saveData/loadData/동기화 코드 원본을 메모(diff 기준 확보).
3. 점검 스크립트는 `vocal_lab_pattern_check.js`, 대상은 발성 2종. 수정 묶음마다 1회 실행.
4. 한 번에 다 합치지 말 것. 아래 STEP 단위로 쪼개서, 각 STEP 끝에 "출력 동일" 확인.

---

## 1. 지금 데이터/동기화가 어떻게 작동하는가 (현 상태 정확히)

### 1-1. 저장 함수 분포 (코드로 확인된 사실)
| 파일 | saveData/loadData | 비고 |
|---|---|---|
| index | 있음(1/1) | 동기화 도장 **없음** |
| home(선생) | 있음(1/1) | 동기화 도장 **있음** ★유일 |
| student(학생홈) | 있음(1/1) | 동기화 도장 없음, SYNC_WATCH 1건 참조 |
| breath(호흡) | 있음(1/1) | 도장 없음. 해시 = rhythm과 동일 |
| rhythm(리듬) | 있음(1/1) | 도장 없음. 해시 = breath와 동일 |
| vocal_lab(발성) | **없음(0/0)** | localStorage **직접 호출** 방식 |
| vocal_lab_male(발성남) | **없음(0/0)** | 〃 |

→ ★중요: "4개 페이지 공통"은 틀린 분류였음. 실제는 **5개 파일에 함수 존재(index/home/student/breath/rhythm)**, **발성 2종은 함수 없이 직접 호출**.

### 1-1-B. ★발성 키 18개 — 저장소·성격·통합대상 (코드 확정)
> 핵심 원칙(사장님 확인): **발성 패턴 데이터는 발성 페이지에서 만들고 선생·학생은 "읽기"만 한다 → 통합 대상 아님.**
> 공통화할 것은 "모든 페이지가 똑같이 쓰는 기반"(피아노 소리=오디오 엔진, 로딩, 날짜)뿐.
> 데이터/동기화에서 공통 후보는 "여러 페이지가 같은 키로 주고받는 공유 데이터"에 한정.
> ※ 패턴 데이터는 각 연습이 소유(대칭): 호흡=`somi_breath_patterns`, 리듬=`somi_rhythm_patterns`, 발성=`somi_vocal_patterns`.
>   각 연습 페이지가 만들고 선생(home)은 읽기만 함 → **전부 통합 대상 아님(소유권이 각 연습에 있음).**

| 키 | 저장소 | 성격 | 통합? |
|---|---|---|---|
| somi_pattern_locks | local | 선생→학생 공유(잠금) | ✅ 공유데이터 |
| somi_pattern_levels | local | 선생→학생 공유(레벨) | ✅ 공유데이터 |
| somi_level_settings | local | 선생→학생 공유(레벨설정) | ✅ 공유데이터 |
| somi_class_settings | local | 선생→학생 공유(반설정) | ✅ 공유데이터 |
| somi_rest_start_dates | local | 선생→학생 공유(휴식진입일) | ✅ 공유데이터 |
| somi_sync_meta | local | 동기화 타임스탬프 | ✅ 공유신호(층2) |
| somi_logs | local | 학생→선생 공유(연습기록) | ✅ 공유데이터 |
| somi_access_log | local | 접근기록 | △ 검토 |
| somi_current_profile_id | local | 현재 로그인 프로필 | △ 공통가능 |
| somi_role | local | 선생/학생 역할 | △ 공통(층2 분기근거) |
| somi_theme | local | 테마(아이보리 등) | △ 공통가능 |
| somi_audio_sr (_SR_KEY) | local | 오디오 샘플레이트 기억 | ◇ 오디오그룹에서 처리 |
| somi_rest_stage_seen_+pid | local | 경계팝업 1회표시(동적키) | ✖ 페이지/프로필별, 그대로 |
| **somi_vocal_patterns** | local | **발성이 생성·소유, 선생(home)이 읽음** | ✖ 통합 안 함(소유=발성) |
| **somi_vocal_open_pat** | **session** | 발성 열린패턴 임시 | ✖ 통합 안 함(+세션) |
| **somi_vocal_scroll** | **session** | 발성 스크롤 위치 | ✖ 통합 안 함(+세션) |
| somi_vocal_preview_gender | local | 발성 성별 미리보기 | ✖ 발성 전용 |
| somi_data | (FB컬렉션명) | localStorage 키 아님 | ✖ 키 아님, 손대지 말 것 |

★통합 시 절대 주의:
- **session 저장소 키(vocal_open_pat, vocal_scroll)를 local용 공통 saveData로 옮기지 말 것** → 동작 깨짐.
- **somi_data는 Firebase 컬렉션 이름**이지 저장 키가 아님 → 키 목록에 넣지 말 것.
- **somi_vocal_* 발성 전용**은 발성이 소유·생성, 다른 페이지는 안 씀 → 독립 유지.
- somi_rest_stage_seen_+pid 는 동적키 → 표준 saveData 일괄교체 대상 아님.

### 1-2. saveData 본문 해시 (동일/차이)
- index: 5be7743d (도장 없음)
- home: c726a713 (★도장 있음 — 유일하게 특별)
- student: 73988c3c (도장 없음)
- breath: b3e57771  ┐ 둘이 완전 동일
- rhythm: b3e57771  ┘
→ home만 특별, breath=rhythm 동일, index·student는 또 각자 다름.

### 1-3. home saveData가 특별한 이유 (보존 필수)
home의 saveData는 저장 시 추가 동작이 있음:
- 저장 값의 키가 `_SYNC_WATCH_KEYS`(아래 목록)에 들어가면 → `somi_sync_meta`에 타임스탬프(ts)를 같이 기록.
- 이게 "선생이 중요한 걸 바꿨다"는 **공유 신호**. 학생 페이지가 이 신호를 보고 동기화함.
- **이 동작을 잃으면 = 선생 변경이 학생에게 반영 안 됨. 잘못 넣으면 = 학생 페이지가 불필요하게 계속 동기화.**

### 1-4. `_SYNC_WATCH_KEYS` (공유 대상 목록 — home에 정의)
```
['somi_pattern_locks','somi_pattern_levels','somi_level_settings',
 'somi_class_settings','somi_rest_start_dates','somi_teacher_stamps']
```
- 정의 위치: home(2건), student(1건 참조). breath/rhythm/발성에는 정의 없음.
- 의미: 잠금·레벨·레벨설정·반설정·휴식진입일·선생도장 = 선생이 바꾸면 학생이 받아야 할 6가지.

### 1-5. 학생 → 선생 공유 경로 (반대 방향)
- 학생이 연습하면 `logPractice`로 `somi_logs`에 기록.
- `_mergeLogs`(breath/rhythm/발성 모두 동일 해시 3e30b78e)로 기기 간 로그 병합.
- `logPractice`는 **페이지마다 해시 다름**(breath 4683129e / rhythm 1849bf73 / vocal 1d0a6adc) → 연습별 차이인지 복붙 어긋남인지 **통합 전 확인 대상**.

### 1-6. 동기화 감지/실행 (학생 페이지)
- `_readSyncTs`: somi_sync_meta의 ts 읽기.
- `_checkAndResync`: 서버(Firebase)에서 최신 받아 반영. breath/rhythm/발성 모두 3건씩 보유(해시는 일부 다름 — 그룹4 동기화 항목).
- 즉 "선생 도장찍기(home) → 학생 감지/재동기화(연습페이지)"가 이미 한 사이클로 돌고 있음. 흩어져 있을 뿐.

---

## 2. 목표 구조 — "통째로 합치기"가 아니라 "역할별 3층 분리"

데이터를 한 함수로 뭉치면 선생/학생 차이가 깨진다. 아래 3층으로 나눠 common_core.js에 둔다.

### 층 1 — 순수 저장/불러오기 (완전 공통)
- 역할: localStorage에 넣고/뺀다. 누가 하든 동일.
- 통합: `saveData(k,v)` / `loadData(k,d)` 기본형을 common_core.js에 1벌.
- 발성 2종도 이 공통 함수를 쓰도록 직접호출을 교체(단, 출력 동일 보장 후).

### 층 2 — 공유 신호 (공통 규칙 + 역할 분기)
- 역할: "선생이 SYNC_WATCH 키를 바꾸면 sync_meta에 ts 기록" + "학생은 ts 보고 재동기화".
- 통합: 도장 찍기 로직을 공통에 두되, **현재 페이지가 선생(home)일 때만** 찍도록 플래그로 분기.
  - 예: `window.__SOMI_IS_TEACHER = true`(home에서만) → 공통 saveData가 이 플래그일 때만 ts 기록.
- 학생 측 `_readSyncTs`/`_checkAndResync`도 공통화(단 그룹4 동기화와 함께, 화면 의존부 분리).

### 층 3 — 공유 대상 목록 (공통)
- 역할: 무엇을 공유로 볼지(`_SYNC_WATCH_KEYS`)를 공통에 1벌.
- 효과: 나중에 "재등록 안내"등 공유 항목 추가 시 **한 곳만** 수정 → 전 페이지 반영.

---

## 3. 작업 STEP (잘게 쪼갬 — 한 STEP씩 점검·확인)

> 각 STEP 끝: 점검 스크립트 1회 + "기능/출력 동일" 육안확인 + present_files. 통과해야 다음 STEP.

- **STEP A. 층3 먼저(가장 안전):** `_SYNC_WATCH_KEYS`를 common_core.js에 정의. home 등 기존 정의는 공통 것을 참조하도록. (값이 100% 동일하므로 출력 무변화)
- **STEP B. 층1 기본 저장:** common_core.js에 표준 saveData/loadData 추가. breath·rhythm은 내용이 동일하므로 공통 호출로 교체(해시 동일 = 안전).
- **STEP C. 층2 공유신호:** 공통 saveData에 "선생 플래그일 때만 ts 기록" 추가. home에 `__SOMI_IS_TEACHER=true` 설정. → home의 특별동작을 공통으로 옮기되 동작 동일.
- **STEP D. index/student 합류:** 두 파일 saveData가 또 다름 → 차이 원인 확인 후 공통+필요분기로 교체. (차이가 의도면 분기, 어긋남이면 표준화)
- **STEP E. 발성 2종 합류 (분류표 1-1-B 따름):** 발성의 직접 localStorage 호출 중 **공유데이터 키(✅)만** 공통 saveData/loadData로 교체.
  - ✖ 제외: somi_vocal_*(발성 전용·발성이 소유), session 키(vocal_open_pat·vocal_scroll), somi_data(FB컬렉션명), somi_rest_stage_seen_+pid(동적키). 이들은 발성에 그대로 둔다.
  - ◇ somi_audio_sr 등 오디오 관련은 데이터그룹이 아니라 오디오 엔진 통합(그룹3)에서 처리.
  - 키 단위로 하나씩, 교체 후 그 키 동작 동일 확인.
- **STEP F. 학생 동기화부:** `_readSyncTs`/`_checkAndResync`/`_mergeLogs` 공통화(그룹4와 연계). 화면 의존 코드는 페이지에 남김.
- **STEP G. logPractice 확인:** 페이지별 해시 차이가 의도인지 검증. 공통화 가능분만 분리, 연습별 차이는 독립 유지.

각 STEP은 독립적으로 되돌릴 수 있게 작은 단위로. 절대 A~G를 한 번에 하지 말 것.

---

## 4. 누락 방지 체크리스트 (작업 끝에 전부 ✔)
- [ ] saveData/loadData: index·home·student·breath·rhythm·발성2종 전부 공통 경유 확인
- [ ] home의 동기화 도장(ts 기록) 동작 유지 — 선생 변경이 학생에 반영되는지 실제 확인
- [ ] _SYNC_WATCH_KEYS 6개 키 그대로(누락·추가 없이)
- [ ] 학생 logPractice → somi_logs → _mergeLogs 경로 정상
- [ ] _checkAndResync 동작(서버 최신 반영) 정상
- [ ] 발성 2종: **공유데이터 키(✅)만** 공통 경유로 교체 — somi_pattern_locks, somi_pattern_levels, somi_level_settings, somi_class_settings, somi_rest_start_dates, somi_sync_meta, somi_logs
- [ ] 발성 전용/세션/FB/동적 키는 **건드리지 않았는지** 확인 — somi_vocal_patterns, somi_vocal_open_pat(session), somi_vocal_scroll(session), somi_vocal_preview_gender, somi_data(FB), somi_rest_stage_seen_+pid, somi_audio_sr
- [ ] session 키가 local로 잘못 옮겨지지 않았는지 (vocal_open_pat·vocal_scroll)
- [ ] loadData breath vs rhythm 차이(73bc4609 vs 82e976dd)가 들여쓰기뿐인지 확인 후 통합
- [ ] 점검 스크립트 발성 2종 통과
- [ ] 화면 출력/기능 이전과 동일(육안)

---

## 5. 위험도 요약
- 층3(키목록): 안전(값 동일).
- 층1(기본저장 breath·rhythm): 안전(해시 동일).
- 층2(선생 도장 분기): 중간(분기 정확히).
- index/student/발성 합류: 중간~주의(각자 다름 → 하나씩).
- logPractice: 확인 필요(차이 정체 규명 후).
