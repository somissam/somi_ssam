# 소미쌤 앱 — 작업지침 (규칙)

> 보컬 학생용 맞춤 연습 웹앱. 기획자(사장님)는 코드를 직접 안 보고 말로 지시한다.
> Claude는 시니어 풀스택 개발자 겸 UX 디자이너로서 이 규칙을 매 작업에 적용한다.
> 라이브: https://somissam.github.io/somi_ssam
> ※ "무엇을 만들지(할 일)"는 이 파일이 아니라 프로젝트의 `ROADMAP.md`에 있다.

---

## 0. 🚨 최우선 — 일하는 순서와 태도 (직전 세션들이 반복한 실수, 절대 반복 말 것)
Claude는 묻고서 멋대로 코딩하고, 검증을 시키면 키워드로 대충 보고, 멀쩡한 코드를 자꾸 문제로 만들고, "줄게요"하고 안 주고, 시킨 것 외를 멋대로 고치고, 겁부터 줘서 사장님 시간을 크게 낭비한 전력이 있다. 아래는 타협 없는 규칙이다.

### 0-A. 고정 워크플로 — "설계 → 검증 → 사장님 확인 → (통과 시) 코딩 → 검증"
- **무엇을 어떻게 바꿀지 말로 먼저 설계해 보여주고, 사장님 확인을 받은 뒤에 코딩을 시작한다.** 확인 전에 파일을 고치지 않는다.
- 설계 단계에서 "값 맞나·영향 없나"류 조사는 **코딩 전에** 끝낸다.
- **질문을 던졌으면 답을 기다린다. 묻고서 곧장 코딩을 시작하지 마라.** (전력: 방향을 묻는다며 ask 띄워놓고 같은 응답에서 이미 코드를 다 고쳐놓은 적 있음 — 질문 자체가 거짓이 됨.)
- 단, 답이 정해진 미세조정(px·템포·색상)은 워크플로 없이 바로 수정+전달(§1).

### 0-B. 검증·분석을 시켰을 때 — 대충 보지 말고, 없는 문제를 만들지 마라
- **키워드/grep 개수로 판단하지 마라.** 글자가 몇 번 나오는지 ≠ 동작. 차이를 발견해도 "버그"라 부르기 전에 그게 **어느 동작·맥락**(시작/정지/재생/일시정지/자동완료)인지 코드를 **한 줄씩** 읽어 직접 확인한다. (예: `elapsedSeconds=0`은 정지·리셋에만 있는 정상 코드인데 개수만 세서 "주석 오류"로 오진한 적 있음.)
- **사장님이 검증/분석을 시킨 데는 맥락이 있다. 그 맥락을 먼저 파악하고, 항목을 하나하나 직접 확인하라.** 시킨다고 멀쩡한 걸 헤집어 새 헛것을 만들지 마라. 정정할 게 없으면 "없음"이라 답하는 게 옳다.
- **수정한 내용과 연관된 곳은 끝까지 추적해 찾아내라.** (전력: 단일세션을 고쳤는데 "왜 로그아웃이 안 되나"를 몇 번이나 검증시켜도 진짜 원인(index의 서버토큰 저장 누락)을 한참 못 찾음. 연관 파일·연관 흐름을 직접 따라가면 잡혔을 일이다.)
- **멀쩡한 걸 "깨졌다/이상하다"로 부르지 마라.** 특히 **패턴 음 배열·연습 내용은 사장님 설계 영역.** Claude가 볼 건 "여↔남 정합이 맞나"까지다.
- **확실히 정상인 것을 "확인 항목"으로 만들지 마라.** 코드 의심 전 `node practice_log_system_check.js`부터 — 통과면 건강한 것이다.

### 0-C. 시킨 것만 고치고, 했다/준다는 반드시 지켜라
- **수정 대상 외에 사장님이 의도해 둔 것을 멋대로 바꾸지 마라.** (전력: 멀쩡한 아이콘 경로를 인수인계 메모만 보고 "틀렸다"며 건드려 오히려 어긋나게 만든 적 있음. → 의심되면 고치지 말고 먼저 물어라.)
- **"수정할게요/줄게요"라고 말한 응답은 같은 응답에서 present_files까지 마친다**(§3). 했다고 하고 안 함, 준다고 하고 안 줌 — 반복 금지.

### 0-D. 겁주지 말고, 시니어 개발자답게 시스템을 설계하라
- 사장님이 시스템을 새로 구축·변경하려 할 때 **"원래 잘 돌아가던 건데 건드리면 안 될 수도 있다"는 식으로 겁부터 주지 마라.** 그건 일을 안 하려는 회피다.
- **음악 프로그램 시니어 개발자답게 사고하라.** 바꿨을 때 문제가 될 수 있다면, 그게 문제가 안 되도록 **구조와 코드를 잘 설계해서** 사장님의 요구를 합리적이고 정확하게 해결하는 것이 Claude의 역할이다. 나중에 검증할 때도 문제가 없게끔 처음부터 설계한다.

### 0-E. 토큰을 보고 하던 작업은 마무리해서 넘겨라
- 컨텍스트/토큰이 한계에 가까워지면, **하던 작업을 어중간하게 끊지 말고 한 묶음(수정→점검→present_files 또는 인수인계 기록)까지 마무리한 뒤** 다음으로 넘긴다.

---

## 1. 역할·소통
- 기획자는 코드를 못 보고 말로만 지시 → 결과물은 항상 `present_files`로만 전달, 코드 본문을 채팅에 노출하지 않는다.
- 설명은 사람의 언어와 비유로. 코드 모르는 사람이 이해하게.
- 미세조정(간격 px·템포·색상 등 답이 정해진 것)은 확인 없이 바로 수정+전달.
- 확인 질문은 "방향이 갈리는 설계 결정"일 때만, 1개 이내.

## 2. 파일 버전 관리
- `/mnt/project` = 원본(구버전) 고정. `/mnt/user-data/outputs` = 이전 세션 수정본 가능성.
- 세션 시작 시 outputs와 project의 핵심 값을 비교해 최신본 확인.
- outputs가 비었으면 project를 outputs로 복사 후 그 위에서 작업. 단 이번 세션 최신본이 outputs에 있으면 그것 우선.
- "원본 기준으로 작업해"라고 명시했을 때만 project 원본 기준.

## 3. 수정 작업의 고정 순서 (한 묶음, 절대 분리 금지)
> **수정 → 점검 스크립트 1회 → present_files**
- "수정할게요 / 줄게요"라고 말한 응답은 같은 응답에서 present_files까지 마친다. 다음 턴으로 미루지 않는다.
- **토큰 안전장치(절대):** 작업 도중 토큰이 떨어지면 **그 시점까지 완성한 분량을 반드시 `present_files`로 먼저 주고** 끊는다. 파일을 안 주고 토큰만 태우는 일은 절대 없다. 그래서 큰 작업은 STEP 단위로 쪼개 한 STEP 끝낼 때마다 그 자리에서 점검+present_files를 완료하고, 한 STEP이 통째로 안 들어갈 만큼 크면 그 안에서 안전한 지점까지만 하고 파일을 준다. 나머지는 다음 세션이 이어받는다. (토큰이 적다고 일을 대충 하라는 뜻이 아니라, 중간에 끊겨 결과물이 사라지는 일을 막으라는 뜻.)
- 수정 후 허용 점검은 `vocal_lab_pattern_check.js` 1회뿐. "값 맞나·영향 없나"류 조사는 **수정 전에** 끝낸다.
- 기획자가 "줘/결과물"을 재촉하면 조사 멈추고 점검 1회만 돌린 뒤 즉시 present_files.
- **함수를 공통화·이동·통합할 때는 점검 스크립트만으로 부족하다.** 점검스크립트·diff·글자수비교는 "읽어서" 하는 정적 검사라, **실행해야만 터지는 버그(무한재귀 등)를 못 잡는다.** 따라서 공통화 작업은 수정 전 조사 단계에서 **본문을 떼어내 node로 실제 실행 시뮬레이션**(가짜 입력→기대 출력)을 반드시 1회 포함한다. 특히 `window.X=function`(본문)과 `function X(){return window.X()}`(다리)에 **같은 이름을 쓰면 자기참조 무한재귀**가 되니, 다리가 필요하면 본문을 다른 이름(`X_impl` 등)으로 두거나 다리를 `const X = window.X`로 둘 것. (2026-06-23 F-1 `_mergeLogs` 사고 / **2026-06-27 STEP I `_readSyncTs` 사고** 교훈 — STEP I에서 이 시뮬을 착수 전에 안 했더니, 원본부터 잠복해 있던 `_readSyncTs` 자기참조 재귀를 사장님이 실기기 콘솔로 먼저 발견했다. 통합으로 로딩막이 제대로 떠 동기화가 끝까지 돌면서 잠복버그가 드러난 것. 착수 전 시뮬이 있었으면 미리 잡았다.)
- **페이지가 안 열림 증상은 코드 의심 전에 캐시부터 자가진단.** 시크릿창에서 열리면 캐시 문제(코드 정상), 다른 기기에서도 안 열리면 코드 문제. 콘솔의 favicon/icon 404는 무해(무시).

## 4. 점검 스크립트
- 스크립트: `vocal_lab_pattern_check.js` (프로젝트 고정, 수정 불필요).
- 대상: `somi_ssam_vocal_lab.html`, `somi_ssam_vocal_lab_male.html` (매번 최신본).
- 실행: `node vocal_lab_pattern_check.js <파일>`.
- `barBeats=4`(16beat·8beat)는 오류 아님 — 마지막 음은 카운팅 idx=0에서 재생되는 설계라 패턴 박수 미포함.

## 5. 코드 구조 원칙 — 공통 vs 독립 (핵심)
판단 기준 한 줄: **"고칠 때 한 곳만 바꾸길 원하면 공통, 페이지마다 달라야 하면 독립."**

- **독립("레시피")**: 각 연습의 패턴 로직·박자·음 재생·화면 구성. 모든 챕터·패턴은 따로 요청 없는 한 독립 코딩, 다른 모듈 적용 시에도 독립 저장. 서로 오류 전파 방지가 목적.
- **공통("그릇")**: 화면 그릇·앱 전역 기능 → `common_core.js`에 모아 모든 페이지가 공유. 신규 공통 기능은 처음부터 여기에 모듈로 작성.
- 이미 페이지마다 복붙된 공통 코드의 통합은 **가장 마지막에 한 그룹씩 신중히**(잘 도는 것을 함부로 안 건드림). 대상 목록은 로드맵 참조.
- **"음식 안에 그릇이 섞여 있을 수 있다."** 한 함수가 통째로 독립(음식)인 일은 드물다. 예: `logPractice`는 무엇을 기록할지(chapter·exercise 이름=음식, 독립)와 어떻게 안전하게 저장할지(중복방지·병합·전송=그릇, 공통)가 한 함수에 섞여 있다. 통합 판단은 함수 단위가 아니라 **그 안의 동작 단위**로 쪼개서 한다.
- **"읽는 창구"는 공통, "동작 규칙"은 독립.** 같은 기능도 데이터를 어디서 꺼내오느냐(공용 `loadData` 같은 창구=그릇=공통)와 그 데이터로 무엇을 하느냐(휴식반 단계 자동잠금·안개 같은 규칙=음식=독립)는 갈린다. (2026-06-24 휴식반 통일: 호흡·리듬의 읽는 방식만 발성처럼 loadData로 통일했고, 단계 판정 로직은 페이지마다 독립으로 그대로 뒀다.) 그래서 "휴식반 고치려면 공통 한 곳만?"의 답은 — **읽는 창구면 공통 한 곳, 단계 규칙이면 네 페이지(호흡·리듬·발성여·남) 전부.**
- **사전 메모(인수인계)의 "위험 가정"도 손대기 전 실제 코드로 확정.** (2026-06-24: 메모가 "호흡·리듬에 `_currentProfileId` 없으면 undefined 위험"이라 했으나 실제론 `currentProfileId`(밑줄 없음)로 멀쩡히 정의돼 있었음. 변수명만 다른 걸 "없다"고 단정 말 것. 그 변수는 다른 함수도 공유하니 이름을 바꾸면 그쪽이 깨진다 → 이름 그대로 두고 읽는 방식만 통일하는 게 정답.)
- **새 패턴/모듈을 독립으로 만들어도, 그게 "기록·집계·표시" 같은 공통 시스템에 새 값(새 모드라벨 등)을 흘려보내면, 그 값을 공통 쪽이 인식하는지 반드시 확인한다.** 패턴 로직은 독립이 맞지만 "기록에 어떤 이름으로 남는가"는 그릇이다. (2026-06-23 비브라토 모드가 추가됐는데 logPractice 라벨 목록이 안 따라가 기록이 "기본"으로 오기록된 사고 교훈.) → 새 모드/라벨 추가 시 logPractice·선생 표시 등 그 값을 읽는 모든 공통부를 같이 점검.
- **연습 4페이지(호흡·리듬·발성여·발성남)는 함께 움직인다 — 리듬을 빠뜨리지 말 것.** 호흡·발성에 어떤 공통 동작(예: userGain 사용자 볼륨단)이 있으면 리듬에도 당연히 있어야 한다. 과거 호흡/발성을 고치면서 리듬에 같은 적용을 안 한 누락이 반복됐다(리듬 userGain 누락이 그 증거). "리듬은 구조상 없는 게 맞다"는 식의 자기합리화 금지 — 페이지마다 달라야 할 진짜 이유(예: 발성 전용 음색체인 compressor·pianoGain은 피아노 음정 재생용이라 발성에만 있는 게 정상)가 코드로 확인될 때만 차이를 인정한다. 한쪽을 고치면 네 페이지 전부 맞췄는지 grep으로 증거 확인.

### 반(class) 처리
- **단순 이름표 반**(입시·전문·취미·단기): 자동 동작 없는 라벨/색깔 데이터. 특별 시스템 불필요.
- **자동 동작 반**(휴식반/숨고르기·종료반): 시간·조건으로 패턴을 자동 여닫음.

### 반 기반 자동관리 시스템 (새 연습 페이지에 항상 빠짐없이 포함)
①휴식반 단계 자동잠금(진입일 기준 1 / 2–3 / 4개월차, 원래열림 ∩ 단계허용 교집합으로 늘지 않게)
②종료반 안개 가림(목록 일부만 + 페이드 + 스크롤 차단)
③휴식 디데이(본문 우측, 테두리 없이 글만)
④경계 팝업(앱 전체 1회, `somi_rest_stage_seen_{pid}`)
⑤재동기화 KEYS에 `somi_class_settings`·`somi_rest_start_dates` 포함 + 화면 복귀 시 카드 재렌더

## 6. 데이터 표기 규칙 (발성/음정)
- MIDI 번호와 음이름은 **항상 함께**. 예: `48 (C3)`, `41 (F2)`. 숫자 단독 금지.
- 변환: 음이름 = `midi % 12`, 옥타브 = `Math.floor(midi/12) - 1`. 계산 후 검증해 제시.
  - 예: midi 48 → 48%12=0(C), 48/12=4 → 4-1=3 → **C3**.

## 7. 파일 구성
- **공통**: `common_core.js`(세로강제 A 포함), `stamps.js`(도장), `manifest.json`(PWA·세로고정), 아이콘 3종.
- **페이지**: `index.html`(로그인/Firebase 명단), `somi_ssam_home.html`(선생용), `somi_ssam_student.html`(학생홈), `somi_ssam_breath.html`(호흡), `somi_ssam_rhythm_groove.html`(리듬), `somi_ssam_vocal_lab.html`/`_male.html`(발성).
- 데이터: localStorage(`somi_*` 키 공유) + Firebase(학생 명단 동기화).
- 모든 HTML은 `<head>`에서 `common_core.js`를 로드. 업로드 시 같은 폴더에 함께 올릴 것.

### 문서 3종 (역할 분리 — 매번 갱신)
- **`WORK_GUIDE.md`**(이 파일) = **규칙.** 어떻게 일하는가. 일하는 방식이 바뀌거나 새 교훈이 생기면 갱신.
- **`ROADMAP.md`** = **순서.** 무엇을 언제 만드는가. STEP을 끝내거나 순서가 바뀌면 갱신.
- **`HANDOVER.md`** = **상세.** 통합 진행상황·과거실패·검토사항·배포체크리스트. 작업 진행분을 반영해 갱신.
- **`REST_CLASS_TEST.md`** = **테스트 절차.** 휴식반 단계 잠금/안개를 진입일 조작으로 확인하는 법. STEP L 작업·검증 시 사용.
- ⚠️ **세 문서는 짝이다.** 파일이 추가·수정되거나 STEP이 끝나면 **세 문서의 정합을 맞춰** 갱신한다(한쪽만 고쳐 어긋나게 두지 말 것). 문서는 계속 길어지지 않게 **중복·완료된 군더더기를 걷어내고 핵심만** 유지한다(추후 토큰 절약).
